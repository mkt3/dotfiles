% gpg

# Import my public key
gpg --fetch-keys https://github.com/mkt3.gpg

# list gpg directory
gpgconf --list-dir

# Trust gpg key
gpg --edit-key "<gpg key id>" trust quit

# list keys
gpg --list-keys

# encrypt file
gpg --encrypt --recipient <public_key> <input_filename>
