% rsync

# Sync directory from <SRC> to <DEST>
rsync -avh --stats --info=progress2 --exclude '**.venv'

# Dry run ync directory from <SRC> to <DEST>
rsync -avhn --stats --info=progress2 --exclude '**.venv'
